@model IEnumerable<CategoryViewModel>
<div class="row g-4">
    <div class="col-lg-12">
        <div class="mb-3">
            <h4>Categories</h4>
            <ul class="list-unstyled fruite-categorie">
                @foreach (CategoryViewModel viewModel in Model)
                {
                    @Html.Partial("~/Views/Shared/Partials/_CategoryItem.cshtml", viewModel)
                }
            </ul>
        </div>
    </div>
</div>