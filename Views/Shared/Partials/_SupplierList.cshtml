@model IEnumerable<SupplierViewModel>
<div class="row g-4">
    <div class="col-lg-12">
        <div class="mb-3">
            <h4>Suppliers</h4>
            <ul class="list-unstyled fruite-categorie">
                @foreach (SupplierViewModel viewModel in Model)
                {
                    @Html.Partial("~/Views/Shared/Partials/_SupplierItem.cshtml", viewModel)
                }
            </ul>
        </div>
    </div>
</div>